<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.haloqlinic.fajarfotocopy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FajarFotocopy">
        <activity android:name=".gudang.DataPertokoGudangActivity"></activity>
        <activity android:name=".gudang.DataTokoGudangActivity" />
        <activity android:name=".gudang.TambahTokoGudangActivity" />
        <activity android:name=".gudang.DetailBarangStockGudangActivity" />
        <activity android:name=".gudang.FilterGudangActivity" />
        <activity android:name=".gudang.CekStockPertokoGudangActivity" />
        <activity android:name=".gudang.CekStockTokoGudangActivity" />
        <activity android:name=".gudang.TokoGudangActivity" />
        <activity android:name=".gudang.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>